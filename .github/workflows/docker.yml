name: build dockerimage 
on: push 
jobs:
 create-image:
   runs-on: ubuntu-latest
   steps:
    - name : checkout
      uses: actions/checkout@v3
    - name : create docker image
      run: docker run -dt --name c1 ubuntu
   
 
